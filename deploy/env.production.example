# Copy to project root as .env.production before: npm run build
# Used by Vite (frontend). Replace with your real domains.

VITE_API_URL=https://api.yourdomain.com/api
VITE_DASHBOARD_URL=https://dashboard.yourdomain.com
